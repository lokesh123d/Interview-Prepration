what is react 
from a react component why we only cna return a one single element 
why we did not push node modules in git hub
why react component name always in a upper case (becuase of baable compiler who say i take a function as a variable when its first name becoem a capital and it will complie accordingly)
what is diffrence between dev dependency and dependency 
diffrence between react component and java script function (babale understanding)
named export and default export 
what is full form of jsx == java script xml
diffrenet between state and props
ternory and if elese condition in react (we did not directly use a if else condition in return js elementn)
jsx and react diffrence can we use sepratley use both of these 
useId hook explain (for generating a new id we can use this while iterating the arry for a key)
default props in react exist 
controlled component (in a component if we have a oen form wiht the diffrent diffrent state and we manage the state usign the state then it called a controlled component)
how we chage the props values becuase pros are immutable (we directly did not chage the value of porps we have to go parent component and chage form where )
useEffect in react 
use transition hooks in react (it will return a time when our function is run and when ir will over we use this when we fetch some data and that time we want to do somthing then we will use this)
what is drive state (we can say the drive state are hold a value of main state like if i have a one array in a state and i hold a first element in a one arrya then it called a drive state )
what is higher order components we use this or not ?
( no we can't use this now)
can we create a nested componet or its use good or not ( we can create this but with the use of this type of code our application slow and sometiems we face an error )





<!-- what is the three phase of event propgration -->


