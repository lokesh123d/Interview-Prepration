we create a context api to follow these steps 
1 . create a one folder context
2. inside the folder add a file and inside the file wrote a createContext and export it and if you want to create a provider at that respective file other wise create a one jsx file and add here a function and inside the function add aone provider of the created context which we create a previous file and after that inside the provider add a childrens for a getting and accessing the data which we are sending and if you are wrote the whole code at a file then create a provider function at that file same to same and insider the context we can share a method or a variable or any thign which we want 
3. and go at main.jsx or App.jsx and wrap a component with the provider it that important and you can provider thing at app.jsx or main but i prefer at a sperated file and go at app or main file and wrap 
4. and after that go to the file wehre you want to get or access the data and use a useContext and inside this wrote which context you want to access and get a data and use it  





